# Environment Configuration Template
# Copy this to .env or .env.production and update values as needed

# Base path for assets in production builds
# Set to "/" for root deployment
# Set to "/apex-task/" for subdirectory deployment (e.g., GitHub Pages project)
# Set to any custom path for your deployment target
VITE_BASE_PATH=/
